package NewsFeedProject.newsfeed.status.entity;

//import jakarta.persistence.EntityListeners; // 필요 없다!

// @EntityListeners(StatusLv2.class) // 필요 없다!
public enum StatusValue {

    stay,acceptance,refusal
}
